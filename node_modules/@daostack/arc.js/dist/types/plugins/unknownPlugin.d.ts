import { Observable } from 'rxjs';
import { Arc, IApolloQueryOptions, IPluginState, Plugin } from '../index';
export declare class UnknownPlugin extends Plugin<IPluginState> {
    static itemMap(context: Arc, item: any, queriedId?: string): IPluginState | null;
    state(apolloQueryOptions?: IApolloQueryOptions): Observable<IPluginState>;
}
