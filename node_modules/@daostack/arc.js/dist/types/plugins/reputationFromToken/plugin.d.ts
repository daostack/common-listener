import { Address, Arc, IPluginState, Operation, Plugin } from '../../index';
export declare class ReputationFromTokenPlugin extends Plugin<IPluginState> {
    static itemMap(context: Arc, item: any, queriedId?: string): IPluginState | null;
    getAgreementHash(): Promise<string>;
    redeem(beneficiary: Address, agreementHash?: string): Operation<undefined>;
    getContract(): Promise<import("ethers").Contract>;
}
